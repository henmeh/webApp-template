import 'package:flutter/material.dart';

class Button5View extends StatelessWidget {
  const Button5View({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Button5View'),
    );
  }
}
